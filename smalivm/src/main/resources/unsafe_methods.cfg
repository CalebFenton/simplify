// These methods will be considered unsafe for reflection. This is useful when a class is white listed as safe but has a few methods which are unsafe.
// Also, adding a clinit signature will prevent the class from being initialized on the smalivm.

// It is unsafe to assume this method will always return the same value.
Ljava/lang/Math;->random()D

// No sleeping on the job please.
Ljava/util/concurrent/TimeUnit;->sleep(J)V

// Initializing these classes cause many other classes to initialize and most of the state is bad.
Ljava/lang/System;-><clinit>()V
Lsun/misc/VM;-><clinit>()V